import { DefaultHost, Host } from '../../hosts'

export interface TSMorphHelperOptions {
  projectRoot: string
  host: Host
}

/**
 * Provides varous ways of createing TSMorph projects and sourcefiles
 */
class TSMorphFactory {
  constructor() {}
}
